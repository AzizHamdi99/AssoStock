import React from 'react'

const page = ({ params }: { params: { id: string } }) => {

    const id = params.id
    console.log(id)
    return (
        <div>

        </div>
    )
}

export default page
